Per eseguire i notebook (file ipynb) con Visual Studio Code occorre installare 
le estensioni per Python e Jupyter Notebook, dopo aver installato nella macchina target Python 3 e pip.

Trovate anche l'export in HTML (da aprire in un browser web) per vedere il codice di esempio con relativo output.